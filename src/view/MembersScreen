package view;

import javax.swing.*;

public class DocumentsScreen extends JPanel {

    public DocumentsScreen() {
        JLabel label = new JLabel("This is the documents screen");
        add(label);
    }

}